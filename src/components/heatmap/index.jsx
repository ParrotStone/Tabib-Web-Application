import React from "react";
import Map from "./map";
// import { Link } from "react-router-dom";

const HeatMap = (props) => {
  return (
    <React.Fragment>
      <Map />
    </React.Fragment>
  );
};

export default HeatMap;
